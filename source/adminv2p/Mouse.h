

 
 //-------------------------  Functions Prototypes  ----------------------//
 
 

 const int initmouse( );                 // Activates the mouse driver

 void showmouse( );                      // Shows the mouse cursor.

 void getmousecoords(int*,int*);         // Returns the current mouse
					 // cursor location.

 const int mouse_key_pressed( );         // Checks the mouse keys ( which
					 // key is pressed. )

 const int leftmousekeypressed( );       // Checks whether left mouse key
					 // is pressed or not.

 const int rightmousekeypressed( );      // Checks whether right mouse key
					 // is pressed or not.

 void hidemouse( );                      // Hides the mouse cursor.

 
